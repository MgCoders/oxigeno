web:
  build: .
  command: php -S 0.0.0.0:8000 -t /code
  ports:
    - "8000:8000"
  links:
    - db
  volumes:
    - .:/code
db:
  image: goja288/oxigeno-web
  environment:
    MYSQL_DATABASE: wordpress
